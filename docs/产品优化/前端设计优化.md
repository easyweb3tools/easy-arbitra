# Easy Arbitra 前端设计优化方案

更新时间：2026-02-24
视角：Apple Design 设计师（HIG 原则 + 金融产品体验）
输入依据：现有前端代码审查 + `docs/产品优化/重构设计.md`

---

## 0. 审查总结

以 Apple Human Interface Guidelines 的三大支柱——**Clarity（清晰）、Deference（顺从）、Depth（层次）**——审视当前前端，识别出以下核心设计债务：

| 维度 | 现状评分 | 核心问题 |
|------|---------|---------|
| 排版层次 | 4/10 | H1 仅 18px，全局字号区间过窄（12-30px），无法建立视觉节奏 |
| 留白与呼吸感 | 3/10 | 卡片间距 16px、内距 20px，信息密度过高，像数据后台而非消费级产品 |
| 深度与层次 | 3/10 | 所有卡片同一 `shadow-sm`，无空间纵深感 |
| 动效与反馈 | 2/10 | 零动画，状态切换无过渡，操作反馈仅靠文字和 opacity |
| 暗色模式 | 0/10 | 不支持，`color-scheme: light` 硬编码 |
| 空状态 | 2/10 | 纯文字 "No AI summary yet"，无引导、无情感 |
| 移动端体验 | 4/10 | 响应式但非移动原生思维，触控目标偏小 |
| 组件一致性 | 4/10 | 无统一组件库，卡片/按钮/徽章在各页面内联定义 |
| 无障碍 | 5/10 | 语义 HTML 基本合格，但缺 focus-visible、ARIA label、动态字号 |
| 图标系统 | 1/10 | 无图标库，仅用 emoji 字符 |

**一句话诊断**：当前 UI 是"能用的工程原型"，不是"想用的消费级产品"。需要从视觉基础层开始系统重建。

---

## 1. 设计哲学：从"数据仪表盘"到"安静的智能助手"

### 1.1 Apple 设计的核心理念应用

**Clarity（清晰）**
> 内容即界面。去掉所有不为内容服务的视觉元素。

当前问题：标签、数字、文字以相近的视觉权重平铺在屏幕上，用户需要主动扫描和解析。

**Deference（顺从）**
> 界面退后，内容走前。流动的动效帮助用户理解而非分散注意力。

当前问题：静态的白色卡片堆叠，没有动效引导用户的注意力流向。

**Depth（层次）**
> 通过半透明层、阴影和动效建立空间感，让用户理解信息的前后关系。

当前问题：所有卡片同一海拔（`shadow-sm`），决策卡和原始数据在视觉上没有主次。

### 1.2 目标体验

```
Apple Stocks App 的质感
  + Robinhood 的决策简洁性
  + Linear 的工程审美
  = Easy Arbitra 的目标
```

用户打开产品的感受应该是：

```
当前："好多数据，我需要找到有用的"
目标："它已经帮我筛好了，我只需要决定"
```

---

## 2. 设计系统重建

### 2.1 色彩系统

#### 问题诊断

当前仅定义 5 个自定义色（`bg: #f5f6f8`, `card: #fff`, `ink: #101418`, `accent: #0f766e`, `muted: #64748b`），语义色依赖 Tailwind 默认的 emerald/blue/amber/rose。问题：

1. 缺少暗色模式色板。
2. 语义色之间缺乏协调——emerald 和 teal 放在一起视觉冲突。
3. 没有中性色阶梯（仅用 slate 的零散色阶）。

#### 重设计方案

采用 **语义化 + 明暗双色** 的 Design Token 体系：

```
tailwind.config.ts 扩展
──────────────────────

colors: {
  // 中性色阶（主结构色）
  surface: {
    primary:   'var(--surface-primary)',     // 页面背景
    secondary: 'var(--surface-secondary)',   // 卡片背景
    tertiary:  'var(--surface-tertiary)',    // 分组/嵌套背景
    elevated:  'var(--surface-elevated)',    // 浮层/弹窗
  },

  // 文字色阶
  label: {
    primary:   'var(--label-primary)',       // 标题、核心数字
    secondary: 'var(--label-secondary)',     // 正文
    tertiary:  'var(--label-tertiary)',      // 辅助说明
    quaternary:'var(--label-quaternary)',    // 占位符
  },

  // 语义色（双色对）
  tint: {
    green:  'var(--tint-green)',   // 正收益、安全、关注
    red:    'var(--tint-red)',     // 亏损、危险、异常
    orange: 'var(--tint-orange)',  // 警告、需关注
    blue:   'var(--tint-blue)',    // 信息、链接、AI
    purple: 'var(--tint-purple)',  // 明星池、高级
    gold:   'var(--tint-gold)',    // 精选、推荐
  },

  // 分割线与边框
  separator: 'var(--separator)',
  border:    'var(--border)',
}
```

**CSS 变量定义**（`globals.css`）：

```css
/* ── Light Mode ── */
:root {
  --surface-primary:   #F2F2F7;
  --surface-secondary: #FFFFFF;
  --surface-tertiary:  #F9F9FB;
  --surface-elevated:  #FFFFFF;

  --label-primary:     #000000;
  --label-secondary:   #3C3C43;
  --label-tertiary:    #3C3C4399;    /* 60% opacity */
  --label-quaternary:  #3C3C4330;    /* 19% opacity */

  --tint-green:   #34C759;
  --tint-red:     #FF3B30;
  --tint-orange:  #FF9500;
  --tint-blue:    #007AFF;
  --tint-purple:  #AF52DE;
  --tint-gold:    #FFD60A;

  --separator:    #3C3C4330;
  --border:       #3C3C431F;
}

/* ── Dark Mode ── */
@media (prefers-color-scheme: dark) {
  :root {
    --surface-primary:   #000000;
    --surface-secondary: #1C1C1E;
    --surface-tertiary:  #2C2C2E;
    --surface-elevated:  #2C2C2E;

    --label-primary:     #FFFFFF;
    --label-secondary:   #EBEBF5;
    --label-tertiary:    #EBEBF599;
    --label-quaternary:  #EBEBF530;

    --tint-green:   #30D158;
    --tint-red:     #FF453A;
    --tint-orange:  #FF9F0A;
    --tint-blue:    #0A84FF;
    --tint-purple:  #BF5AF2;
    --tint-gold:    #FFD60A;

    --separator:    #54545899;
    --border:       #5454584D;
  }
}
```

**对现有代码的影响**：

| 当前用法 | 替换为 |
|---------|--------|
| `bg-bg` (#f5f6f8) | `bg-surface-primary` |
| `bg-card` (#fff) | `bg-surface-secondary` |
| `text-ink` (#101418) | `text-label-primary` |
| `text-muted` (#64748b) | `text-label-tertiary` |
| `bg-accent` (#0f766e) | `bg-tint-blue`（主操作统一为蓝色） |
| `bg-emerald-*` | `bg-tint-green/*` 系列 |
| `bg-amber-*` | `bg-tint-orange/*` 系列 |
| `text-rose-700` | `text-tint-red` |

### 2.2 排版系统

#### 问题诊断

当前排版：
- H1（页面标题）= `text-lg`（18px）`font-semibold`
- H2（区块标题）= `text-lg`（18px）`font-semibold`
- H3（子标题）= `text-base`（16px）`font-semibold`
- 正文 = `text-sm`（14px）
- 辅助 = `text-xs`（12px）
- 大数字 = `text-3xl`（30px）

**问题**：H1 和 H2 大小相同（都是 18px），层级不清。整体字号区间仅 12px-30px，层级不够。

#### 重设计方案

参照 Apple HIG 的 Dynamic Type 层级，建立 **8 级排版阶梯**：

```
排版阶梯（基于 8pt 网格）
─────────────────────────

Level          Size    Weight      Line-H    Letter-Sp   用途
─────          ────    ──────      ──────    ─────────   ────
Large Title    34px    Bold        41px      0.37px      首页 Hero 区
Title 1        28px    Bold        34px      0.36px      页面标题
Title 2        22px    Bold        28px      0.35px      区块标题
Title 3        20px    Semibold    25px      0.38px      卡片标题
Headline       17px    Semibold    22px      -0.41px     列表行主标题
Body           17px    Regular     22px      -0.41px     正文
Callout        16px    Regular     21px      -0.32px     辅助正文
Subheadline    15px    Regular     20px      -0.24px     列表行副标题
Footnote       13px    Regular     18px      -0.08px     时间戳、元数据
Caption 1      12px    Regular     16px      0           标签、徽章
Caption 2      11px    Regular     13px      0.07px      最小合法文字
```

**Tailwind 扩展**（`tailwind.config.ts`）：

```js
fontSize: {
  'large-title': ['34px', { lineHeight: '41px', letterSpacing: '0.37px', fontWeight: '700' }],
  'title-1':     ['28px', { lineHeight: '34px', letterSpacing: '0.36px', fontWeight: '700' }],
  'title-2':     ['22px', { lineHeight: '28px', letterSpacing: '0.35px', fontWeight: '700' }],
  'title-3':     ['20px', { lineHeight: '25px', letterSpacing: '0.38px', fontWeight: '600' }],
  'headline':    ['17px', { lineHeight: '22px', letterSpacing: '-0.41px', fontWeight: '600' }],
  'body':        ['17px', { lineHeight: '22px', letterSpacing: '-0.41px', fontWeight: '400' }],
  'callout':     ['16px', { lineHeight: '21px', letterSpacing: '-0.32px', fontWeight: '400' }],
  'subheadline': ['15px', { lineHeight: '20px', letterSpacing: '-0.24px', fontWeight: '400' }],
  'footnote':    ['13px', { lineHeight: '18px', letterSpacing: '-0.08px', fontWeight: '400' }],
  'caption-1':   ['12px', { lineHeight: '16px', fontWeight: '400' }],
  'caption-2':   ['11px', { lineHeight: '13px', letterSpacing: '0.07px', fontWeight: '400' }],
}
```

**当前代码映射**：

| 当前 | 替换为 | 位置 |
|------|--------|------|
| 首页统计标题 `text-3xl font-semibold` | `text-large-title` | `app/page.tsx` |
| 页面标题 `text-lg font-semibold` (H1) | `text-title-1` | 所有 page.tsx |
| 区块标题 `text-lg font-semibold` (H2) | `text-title-2` | 所有 article 标题 |
| 卡片标题 `text-base font-semibold` (H3) | `text-title-3` | 子区块 |
| 列表行主文字 `text-sm font-medium` | `text-headline` | 钱包名、市场名 |
| 正文 `text-sm` | `text-body` | 描述、摘要 |
| 元数据 `text-xs text-muted` | `text-footnote text-label-tertiary` | 时间、统计 |
| 标签 `text-xs font-medium` | `text-caption-1 font-medium` | 徽章文字 |

#### 字体选择

当前使用 IBM Plex Sans。建议保留但调整 fallback 栈，确保 Apple 设备优先使用系统字体：

```css
font-family:
  -apple-system,
  BlinkMacSystemFont,
  'IBM Plex Sans',
  'SF Pro Display',
  'Helvetica Neue',
  sans-serif;
```

等宽字体（用于地址、JSON 显示）：

```css
font-family:
  'SF Mono',
  'IBM Plex Mono',
  'Menlo',
  monospace;
```

### 2.3 间距系统

#### 问题诊断

当前间距随意使用 Tailwind 的 `p-3`、`p-4`、`p-5`、`space-y-2`、`space-y-4`，没有统一节奏。

Apple 的间距原则：**以 8pt 为基础单位，所有间距是 8 的倍数（4pt 用于微间距）。**

#### 重设计方案

```
间距阶梯
────────

Token         Value    用途
─────         ─────    ────
space-0       0
space-1       4px      图标与文字间距、徽章内距
space-2       8px      紧凑行间距、小组件内距
space-3       12px     列表项内距
space-4       16px     卡片内距（紧凑）、节间距
space-5       20px     卡片内距（标准）
space-6       24px     区块间距
space-8       32px     页面大区块间距
space-10      40px     页面边距、Hero 区上下
space-12      48px     页面顶部间距
space-16      64px     大分区间隔
```

**关键修改**：

| 当前 | 问题 | 替换为 |
|------|------|--------|
| 卡片 `p-5`（20px） | 内容与边框太近 | `p-6`（24px）+ 内部 `space-y-4`（16px） |
| 列表项 `p-3`（12px） | 偏紧 | `px-4 py-3`（16px x 12px） |
| 区块间距 `space-y-4`（16px） | 区块间没有呼吸感 | `space-y-6`（24px）或 `space-y-8`（32px） |
| 页面边距 `px-6 py-6` | 整体偏紧 | `px-6 py-8 lg:px-10 lg:py-10` |
| 按钮内距 `px-4 py-2` | 偏小 | `px-5 py-2.5`（20px x 10px），最小触控 44px 高 |

### 2.4 圆角系统

#### 问题诊断

当前混用 `rounded-md`（6px）和 `rounded-lg`（8px），无体系。

Apple 使用 **continuous corner**（连续曲线圆角），且圆角大小与组件大小成正比。

#### 重设计方案

```
圆角阶梯
────────

Token        Value      用途
─────        ─────      ────
radius-sm    8px        徽章、小标签、按钮内元素
radius-md    12px       按钮、输入框、列表项
radius-lg    16px       卡片、面板
radius-xl    20px       大卡片、弹窗
radius-full  9999px     圆形头像、胶囊按钮
```

**Tailwind 扩展**：

```js
borderRadius: {
  sm:   '8px',
  md:   '12px',
  lg:   '16px',
  xl:   '20px',
  full: '9999px',
}
```

**关键修改**：

| 当前 | 替换为 |
|------|--------|
| 卡片 `rounded-lg`（8px） | `rounded-lg`（新 16px） |
| 列表项 `rounded-md`（6px） | `rounded-md`（新 12px） |
| 按钮 `rounded-md`（6px） | `rounded-md`（新 12px） |
| 徽章 `rounded`（4px） | `rounded-sm`（新 8px） |
| 语言切换 `rounded-md` | `rounded-full`（胶囊形） |

### 2.5 阴影与层次系统

#### 问题诊断

全局仅使用 `shadow-sm`，所有卡片处于同一视觉海拔。Apple 的设计通过不同的阴影层级表达信息的重要性。

#### 重设计方案

```
海拔阶梯
────────

Level       Shadow                                          用途
─────       ──────                                          ────
Level 0     none                                            嵌入式元素（列表行）
Level 1     0 1px 3px rgba(0,0,0,0.04),                     常规卡片
            0 1px 2px rgba(0,0,0,0.06)
Level 2     0 4px 12px rgba(0,0,0,0.06),                    突出卡片（决策卡）
            0 1px 3px rgba(0,0,0,0.04)
Level 3     0 8px 24px rgba(0,0,0,0.08),                    浮层、弹窗
            0 2px 6px rgba(0,0,0,0.04)
Level 4     0 16px 48px rgba(0,0,0,0.12),                   模态对话框
            0 4px 12px rgba(0,0,0,0.06)
```

**Tailwind 扩展**：

```js
boxShadow: {
  'elevation-0': 'none',
  'elevation-1': '0 1px 3px rgba(0,0,0,0.04), 0 1px 2px rgba(0,0,0,0.06)',
  'elevation-2': '0 4px 12px rgba(0,0,0,0.06), 0 1px 3px rgba(0,0,0,0.04)',
  'elevation-3': '0 8px 24px rgba(0,0,0,0.08), 0 2px 6px rgba(0,0,0,0.04)',
  'elevation-4': '0 16px 48px rgba(0,0,0,0.12), 0 4px 12px rgba(0,0,0,0.06)',
}
```

**关键修改**：

| 元素 | 当前 | 替换为 |
|------|------|--------|
| 普通卡片 | `shadow-sm` | `shadow-elevation-1` |
| 决策卡、精选卡 | `shadow-sm` | `shadow-elevation-2` |
| Header/导航 | 无阴影 | `shadow-elevation-1` |
| 弹出菜单 | 不存在 | `shadow-elevation-3` |
| 分享预览弹窗 | 同卡片 | `shadow-elevation-3` |

---

## 3. 组件设计规范

### 3.1 导航栏（Header）

#### 当前问题

```
现状：
┌───────────────────────────────────────────────┐
│ Easy Arbitra · subtitle            [EN] [中文] │
│───────────────────────────────────────────────│
```

- 标题字号偏小，品牌感弱。
- 语言切换器视觉权重过高，与标题争夺注意力。
- 背景 `bg-white/90 backdrop-blur` 是对的思路，但 blur 没有搭配分割线的视觉弱化。

#### Apple 风格重设计

```
桌面端（md+）：
┌───────────────────────────────────────────────────────┐
│                                                       │
│  Easy Arbitra                           EN · 中文   │
│  AI-Powered Smart Wallet Tracker                      │
│                                                       │
│  ────  ────  ──────  ────                             │
│  发现   探索   我的跟踪  更多                             │
│                                                       │
└───────────────────────────────────────────────────────┘

移动端：
┌─────────────────────────────┐
│  Easy Arbitra        EN·中文 │
└─────────────────────────────┘
      ...页面内容...
┌─────────────────────────────┐
│  🏠      🔍      ⭐      ···  │
│  发现    探索    跟踪    更多  │
└─────────────────────────────┘
```

**设计规格**：

```
Header 容器：
  height: 自适应
  background: var(--surface-secondary) / 80% opacity
  backdrop-filter: saturate(180%) blur(20px)
  border-bottom: 0.5px solid var(--separator)
  padding: 16px 24px (桌面) / 12px 16px (移动)
  position: sticky; top: 0; z-index: 50

品牌标题：
  字体: text-title-2 (22px Bold) → 主标题
  字体: text-footnote text-label-tertiary → 副标题

语言切换器：
  形态: 胶囊 Segmented Control
  height: 32px
  border-radius: full
  background: var(--surface-tertiary)
  active segment: var(--surface-secondary) + shadow-elevation-1
  transition: all 200ms cubic-bezier(0.25, 0.1, 0.25, 1)
```

**移动端 Tab Bar**：

```
容器：
  position: fixed; bottom: 0
  height: 83px (含 safe area)
  padding-bottom: env(safe-area-inset-bottom)
  background: var(--surface-secondary) / 80% opacity
  backdrop-filter: saturate(180%) blur(20px)
  border-top: 0.5px solid var(--separator)

Tab Item：
  min-width: 25%
  图标: 24x24, 居中
  文字: text-caption-2 (11px), 图标下方 2px
  active: text-tint-blue + filled icon
  inactive: text-label-tertiary + outlined icon
```

**对现有代码的影响**：
- `components/Header.tsx`：重构，集成品牌区 + 桌面导航。
- `components/Sidebar.tsx`：桌面端功能迁入 Header，移动端重构为底部 Tab Bar。
- `app/layout.tsx`：布局结构从 sidebar+content 改为 header+content+tabbar。

### 3.2 卡片组件

#### 当前问题

所有卡片使用同一模板 `rounded-lg bg-card p-5 shadow-sm`，无法区分重要性。列表项 `rounded-md border border-slate-200 p-3 hover:bg-slate-50` 视觉上像表单元素。

#### Apple 风格重设计

定义 **4 种卡片变体**：

**Variant 1: Grouped Card（列表分组卡）**
```
用途：钱包列表、事件流
─────────────────────────────
background: var(--surface-secondary)
border-radius: 16px
shadow: elevation-1
padding: 0（内部行自带 padding）
overflow: hidden

内部行：
  padding: 14px 16px
  border-bottom: 0.5px solid var(--separator)（最后一行无）
  hover: background var(--surface-tertiary)
  transition: background 150ms ease
  min-height: 44px（Apple 触控标准）
```

**Variant 2: Inset Grouped Card（嵌入式分组卡）**
```
用途：筛选器面板、设置面板
─────────────────────────────
background: var(--surface-secondary)
border-radius: 16px
shadow: none
margin: 0 16px（左右留边）
padding: 0
```

**Variant 3: Prominent Card（突出卡片）**
```
用途：决策卡、今日精选、组合包
─────────────────────────────
background: var(--surface-secondary)
border-radius: 20px
shadow: elevation-2
padding: 24px
border: none
```

**Variant 4: Material Card（材质卡片）**
```
用途：Header、Tab Bar、浮层
─────────────────────────────
background: var(--surface-secondary) / 80% opacity
backdrop-filter: saturate(180%) blur(20px)
border: 0.5px solid var(--separator)
```

### 3.3 按钮系统

#### 当前问题

按钮有 4 种不同样式散落各处，大小不一致：
- Primary: `bg-accent px-4 py-2 text-sm`
- Watch: `bg-emerald-600/bg-slate-600 px-3 py-1 text-xs`
- Secondary: `border border-slate-300 px-2 py-1 text-xs`
- Language: `px-2 py-1 text-xs`

没有统一的触控目标尺寸、状态反馈和交互动效。

#### Apple 风格重设计

```
按钮尺寸标准（高度 = 最小触控目标）
────────────────────────────────

Size      Height    Padding-X    Font          用途
────      ──────    ─────────    ────          ────
Large     50px      24px         headline      页面主操作（关注、分享）
Medium    44px      20px         body          表单提交、次要操作
Small     36px      16px         subheadline   卡片内操作、筛选
Mini      28px      12px         caption-1     标签切换、紧凑操作
```

**按钮变体**：

```
Filled（实心）—— 主操作
  background: var(--tint-blue)
  color: #FFFFFF
  border-radius: radius-md (12px)
  font-weight: 600
  hover: brightness(1.1)
  active: brightness(0.9) + scale(0.98)
  transition: all 200ms cubic-bezier(0.25, 0.1, 0.25, 1)

Tinted（浅底）—— 次要操作
  background: var(--tint-blue) / 12% opacity
  color: var(--tint-blue)
  border-radius: radius-md
  hover: opacity 15%
  active: opacity 20% + scale(0.98)

Gray（灰色）—— 中性操作
  background: var(--surface-tertiary)
  color: var(--label-primary)
  border-radius: radius-md
  hover: var(--surface-tertiary) brightness(0.95)

Plain（文字）—— 链接式操作
  background: transparent
  color: var(--tint-blue)
  padding: 0
  hover: opacity 0.7

Destructive（危险）—— 取消关注、删除
  同 Filled 但 background: var(--tint-red)
```

**按钮状态**：

```
Disabled:
  opacity: 0.35（Apple 标准，不是 0.6）
  pointer-events: none

Loading:
  内容替换为旋转器（14px spinner）
  保持按钮宽度不变（min-width 锁定）
  opacity: 0.7

Pressed:
  scale: 0.98
  brightness: 0.9
  transition: 100ms
```

**对现有代码的影响**：

| 当前按钮 | 映射为 | 文件 |
|---------|--------|------|
| Apply 筛选按钮 | Medium / Filled | `wallets/page.tsx` |
| Watch/Unwatch 按钮 | Small / Tinted (green) | `WatchlistToggleButton.tsx` |
| Trigger AI Analysis | Medium / Filled | `TriggerAnalysisButton.tsx` |
| Copy Share URL | Small / Gray | `ShareCardPanel.tsx` |
| Acknowledge 按钮 | Small / Gray | `AcknowledgeButton.tsx` |
| 语言切换 | Segmented Control（非按钮） | `LanguageSwitcher.tsx` |

### 3.4 徽章 / 标签系统

#### 当前问题

徽章用 `rounded bg-{color}-100 px-2 py-0.5 text-xs font-medium text-{color}-700`，所有徽章视觉权重相同，无法快速区分。

#### Apple 风格重设计

```
徽章变体
────────

Tier Badge（池等级）—— 最高视觉权重
  明星池: background gradient(135deg, #FFD60A, #FF9F0A)
          text: #000, font-weight: 700
          shadow: 0 2px 8px rgba(255,214,10,0.3)
          height: 24px, padding: 0 10px
          border-radius: full

  策略池: background: var(--surface-tertiary)
          border: 1px solid var(--separator)
          text: var(--label-secondary), font-weight: 600
          height: 24px

  观察池: background: none
          text: var(--label-tertiary), font-weight: 400
          height: 24px

Status Badge（状态标签）
  正收益: 左侧小圆点 6px var(--tint-green) + text
  亏损:   左侧小圆点 6px var(--tint-red) + text
  持平:   左侧小圆点 6px var(--label-quaternary) + text

Category Tag（分类标签）
  background: var(--tint-blue) / 10%
  text: var(--tint-blue)
  height: 24px, padding: 0 10px
  border-radius: full
  font: caption-1 medium
```

### 3.5 数据展示组件

#### 当前问题

数字和指标用 `text-3xl font-semibold` 或 `text-sm` 直接展示，没有统一的 stat 组件。正负值没有颜色区分。原始 JSON 用 `<pre>` 直接裸露展示。

#### Apple 风格重设计

**Stat Cell 组件**：

```
┌─────────────────────┐
│  7D Return          │   ← label: caption-1, label-tertiary
│  +12.3%             │   ← value: title-2, tint-green (正) / tint-red (负)
│  ▲ 2.1% vs last wk  │   ← delta: caption-1, tint-green, 带方向图标
└─────────────────────┘

规格：
  padding: 16px
  background: var(--surface-secondary)
  border-radius: 16px
  内部间距: label → value 4px, value → delta 2px
  正值色: var(--tint-green)
  负值色: var(--tint-red)
  零值色: var(--label-tertiary)
```

**进度条 / 风险等级**：

```
风险等级展示（替代纯文字 "低/中/高"）：
┌──────────────────────────────┐
│  Risk Level                  │
│  ████░░░░░░  Low             │
│              ↑               │
│         40% filled,          │
│         color: tint-green    │
└──────────────────────────────┘

规格：
  轨道: height 6px, border-radius full, bg var(--surface-tertiary)
  填充: border-radius full, bg 语义色
  低 (0-33%): tint-green
  中 (34-66%): tint-orange
  高 (67-100%): tint-red
  动画: width transition 600ms ease-out
```

**JSON/证据展示**：

当前 `<pre className="overflow-auto rounded bg-slate-100 p-2 text-xs text-slate-700">` 直接暴露原始 JSON。

重设计：
```
默认折叠，标题行展示摘要信息：
┌──────────────────────────────┐
│  ▶ Raw Evidence  ·  3 items  │  ← 折叠态
└──────────────────────────────┘

┌──────────────────────────────┐
│  ▼ Raw Evidence  ·  3 items  │  ← 展开态
│  ┌──────────────────────────┐│
│  │ {                        ││  ← monospace, surface-tertiary bg
│  │   "key": "value",       ││     syntax highlighting（可选）
│  │   ...                    ││
│  │ }                        ││
│  └──────────────────────────┘│
│                   [Copy JSON] │  ← 右下角复制按钮
└──────────────────────────────┘

折叠/展开动画：
  max-height transition 300ms cubic-bezier(0.25, 0.1, 0.25, 1)
  opacity transition 200ms ease
```

---

## 4. 页面级设计优化

### 4.1 首页

#### 当前问题

首页由 3 个统计卡片 + 两个列表组成，像运维面板。首屏给出"Indexed Markets: 247"，对跟单用户无意义。

#### Apple 风格重设计

参照重构设计文档的首页方案，以 Apple 审美实现：

```
首页信息架构
────────────

Section 1: 今日精选（Prominent Card × 3 水平滚动）
  卡片规格：
    width: 280px (固定)
    border-radius: 20px
    shadow: elevation-2
    padding: 20px
    水平滚动: snap scroll, scroll-snap-type: x mandatory
    每张卡片: scroll-snap-align: start
    间距: 12px

  卡片内容层次：
    Line 1: 池等级徽章（Tier Badge）+ 策略标签（Category Tag）
    Line 2: 钱包名 text-headline
    Line 3: 地址 text-caption-1 text-label-tertiary (truncated)
    Line 4: ──────── separator
    Line 5: 2x2 Stat Grid（7D Return / Drawdown / Score / Risk）
    Line 6: 一句话推荐理由 text-footnote text-label-secondary
    Line 7: ──────── separator
    Line 8: [关注] 按钮 (Filled/Small) 全宽

Section 2: 实时信号（Grouped Card，列表形式）
  标题: text-title-3 + "查看全部 >" Plain 按钮
  每行：
    左: 信号图标 (24x24 圆形底色) + 标题 + 时间
    右: 箭头指示器 >
    底: 0.5px separator，左边距 56px（图标宽度 + 间距）

  信号图标映射：
    收益跃迁: tint-green 底 + 上箭头
    异常回撤: tint-red 底 + 下箭头
    新 AI 报告: tint-blue 底 + 文档图标
    策略漂移: tint-orange 底 + 刷新图标

Section 3: 组合包（2 列 Grid，Prominent Card）
  每张卡片：
    标题 text-title-3
    描述 text-subheadline text-label-secondary
    3 个关键数字（inline stat）
    [一键关注全部] 按钮 (Tinted/Medium)

页面间距：
  Section → Section: 32px
  Section Title → Content: 16px
  页面顶部: 8px（Header 已有间距）
  页面底部: 40px + safe-area
```

### 4.2 探索页（合并后的钱包列表 + 排行榜）

#### Apple 风格重设计

```
页面结构
────────

Area 1: 搜索栏（sticky，Header 下方）
  ┌───────────────────────────────────┐
  │  🔍  搜索钱包地址或名称...          │
  └───────────────────────────────────┘

  规格：
    height: 44px
    border-radius: 12px
    background: var(--surface-tertiary)
    padding-left: 36px（给搜索图标留空间）
    font: body
    position: sticky; top: header-height

Area 2: 策略 Tab（水平滚动 Segmented）
  ┌────┐ ┌────┐ ┌──────┐ ┌──────┐ ┌────┐ ┌────┐
  │ All │ │ MM │ │ Event │ │ Quant │ │ Arb │ │ ... │
  └────┘ └────┘ └──────┘ └──────┘ └────┘ └────┘

  规格：
    height: 36px per tab
    border-radius: full（胶囊）
    active: var(--tint-blue) bg + white text
    inactive: transparent bg + var(--label-secondary)
    水平滚动: overflow-x auto, 隐藏滚动条
    padding: 0 16px（首尾间距）
    间距: 8px

Area 3: 排序 Chip Bar
  ┌──────┐ ┌───────┐ ┌─────┐ ┌──────┐
  │ Score▼│ │ 7D PnL │ │ Risk │ │ Tier │
  └──────┘ └───────┘ └─────┘ └──────┘

  规格：
    形态: 小型 chip
    height: 28px
    border-radius: full
    selected: border 1px tint-blue + text tint-blue
    unselected: border 1px separator + text label-secondary
    可展开为 dropdown

Area 4: 钱包列表（Grouped Card）
  ┌──────────────────────────────────────────┐
  │  ┌──────────────────────────────────────┐│
  │  │ 🥇 WhaleMaster    [明星池]  [做市策略] ││
  │  │                                      ││
  │  │ 0x1a2b...3c4d                        ││
  │  │                                      ││
  │  │ 7D: +12.3%   回撤: -2.8%   Score: 85 ││
  │  │                                      ││
  │  │ "连续30天正收益的做市专家"              ││
  │  │                                      ││
  │  │              [关注]        >          ││
  │  │──────────────────────────────────────││
  │  │ #2 EventHunter    [策略池]  [事件驱动] ││
  │  │ ...                                  ││
  │  └──────────────────────────────────────┘│
  └──────────────────────────────────────────┘

  列表行规格：
    padding: 16px
    min-height: 88px
    separator: 左缩进 16px
    右侧: chevron > 图标（label-quaternary）
    hover: background var(--surface-tertiary)

  信息层次（从上到下）：
    Row 1: 排名 + 名称(headline) + Tier Badge + Strategy Tag
    Row 2: 地址(caption-1, label-tertiary, monospace)
    Row 3: 3 个 inline stat（footnote），正负值着色
    Row 4: AI 摘要（footnote, label-secondary, 1 行截断 + ellipsis）
    Row 5: 操作区 [关注按钮 Tinted/Small]
```

### 4.3 钱包详情页

#### Apple 风格重设计

遵循重构设计文档"决策面板"方案，以 Apple 视觉语言实现：

```
页面结构（纵向滚动）
────────────────────

Section 0: 导航栏（inline，非全局 Header）
  ← 返回探索      ···（更多操作菜单）

Section 1: Profile Header
  ┌──────────────────────────────────┐
  │                                  │
  │  [明星池]                         │
  │                                  │
  │  WhaleMaster                     │  ← title-1
  │  0x1a2b...3c4d  📋               │  ← caption-1, monospace + 复制按钮
  │                                  │
  │  做市策略 · Score 85 · 信息优势中等  │  ← subheadline, label-secondary
  │                                  │
  └──────────────────────────────────┘
  （无边框，直接融入背景）

Section 2: 决策卡（Prominent Card）
  ┌──────────────────────────────────┐
  │                                  │
  │  Verdict                         │  ← caption-1, uppercase, label-tertiary
  │                                  │
  │  适合稳健型投资者                   │  ← title-3
  │  建议配置 5-10% 仓位               │  ← body, label-secondary
  │                                  │
  │  ┌──────┐ ┌──────┐ ┌──────┐     │
  │  │ 风险   │ │ 状态   │ │ 稳定性  │  │  ← 3 个 Stat Cell
  │  │ ██░░░ │ │  平稳  │ │ 92    │  │
  │  │ Low   │ │ Stable │ │ /100  │  │
  │  └──────┘ └──────┘ └──────┘     │
  │                                  │
  │  "该钱包以做市策略为主，连续 30     │  ← callout, label-secondary
  │   天正收益，回撤控制在 3% 以内。    │     line-clamp-3
  │   适合作为底仓配置。"              │
  │                                  │
  │  ┌────────────┐  ┌──────────┐   │
  │  │  ⭐ 关注     │  │  ↗ 分享   │   │  ← Filled/Large + Gray/Large
  │  └────────────┘  └──────────┘   │
  │                                  │
  │  ⚠ 以上为 AI 概率判断，不构成       │  ← caption-1, label-tertiary
  │  投资建议。过往表现不代表未来收益。   │
  │                                  │
  └──────────────────────────────────┘

Section 3: 表现概览（Grouped Card, 2x3 Stat Grid）
  ┌──────────────────────────────────┐
  │  Performance                     │
  │  ┌────────┐ ┌────────┐          │
  │  │ 7D PnL  │ │ 30D PnL │         │
  │  │ +12.3%  │ │ +35.8%  │         │
  │  ├────────┤ ├────────┤          │
  │  │ Volume  │ │ Trades  │         │
  │  │ $1.2M   │ │ 847     │         │
  │  ├────────┤ ├────────┤          │
  │  │ Fees    │ │ Rebates │         │
  │  │ -$234   │ │ +$1.8K  │         │
  │  └────────┘ └────────┘          │
  └──────────────────────────────────┘

Section 4: 近期动态（Grouped Card, 列表）
  （同首页信号列表，带图标+时间+摘要，可点击展开）

Section 5+: 折叠区（独立 Disclosure Groups）
  ▶ 策略归因详情
  ▶ 完整 AI 报告
  ▶ 原始证据

  每个折叠区：
    header: 44px, padding 16px, label-primary, chevron 旋转动画
    content: padding 16px, 展开动画 300ms
    展开图标: 90° 旋转过渡
    separator: 全宽

Section 6: 免责声明（固定在折叠区下方）
  background: var(--tint-orange) / 8%
  border-radius: 16px
  padding: 16px
  text: caption-1, tint-orange
  无边框
```

### 4.4 我的跟踪（Watchlist 升级）

参照重构设计文档方案，Apple 视觉实现要点：

```
Section 1: 组合摘要条（Material Card，sticky 在 Header 下方）
  ┌────────────────────────────────────────┐
  │  5 Following  ·  🟢 4 OK  🟡 1 Alert  │
  └────────────────────────────────────────┘
  height: 44px, 水平居中，caption-1
  background: surface-secondary/80% + blur

Section 2: 需要关注（Prominent Card，tint-orange 左边线）
  左侧 3px 竖线: var(--tint-orange)
  每行内容同探索页列表行，但增加：
    建议文案: callout, tint-orange
    操作按钮: [查看] Tinted/Small + [取消关注] Plain/Small/Destructive

Section 3: 正常更新（Grouped Card，标准列表）
  同首页信号列表样式
  无操作按钮，只有导航箭头 >
```

---

## 5. 动效系统

### 5.1 问题诊断

当前产品 **零动画**。所有状态切换都是瞬间发生的——页面加载直接显示、按钮状态直接切换、折叠直接展开。这在 Apple 设计中是最大的违规之一。

Apple 的原则：**动效不是装饰，是信息。** 它帮助用户理解界面的空间关系和因果关系。

### 5.2 全局过渡 Token

```css
/* globals.css */

:root {
  /* 交互反馈（最快，让用户感到"立即响应"） */
  --duration-instant:  100ms;
  --ease-instant:      cubic-bezier(0.25, 0.1, 0.25, 1);

  /* 元素状态变化（中速，hover/focus/active） */
  --duration-fast:     200ms;
  --ease-standard:     cubic-bezier(0.25, 0.1, 0.25, 1);

  /* 布局变化（展开/折叠/出现/消失） */
  --duration-normal:   300ms;
  --ease-decelerate:   cubic-bezier(0.0, 0.0, 0.2, 1);   /* 进入 */
  --ease-accelerate:   cubic-bezier(0.4, 0.0, 1, 1);     /* 退出 */

  /* 弹性动画（引起注意，如成功反馈） */
  --duration-spring:   500ms;
  --ease-spring:       cubic-bezier(0.34, 1.56, 0.64, 1);
}
```

### 5.3 具体动效清单

| 场景 | 当前 | 重设计 |
|------|------|--------|
| 页面切换 | 无过渡 | View Transition API: 渐变 200ms |
| 卡片 hover | `hover:bg-slate-50`（instant） | background 150ms ease + subtle scale(1.005) |
| 按钮按下 | 无 | scale(0.97) 100ms + brightness 0.9 |
| 关注/取消关注 | 文字切换 "Saving..." | 图标 morph 动画（空心→实心）300ms spring |
| Tier Badge 出现 | 无 | scale(0→1) + opacity(0→1) 300ms spring |
| 折叠展开 | `<details>` 原生（无动画） | max-height + opacity 300ms decelerate |
| 列表项加载 | 全部同时出现 | 逐行延迟淡入: delay = index × 50ms, duration 300ms |
| 数字变化 | 直接显示 | 数字滚动动画（countUp），duration 600ms |
| Toast 通知（复制成功） | 文字颜色变化 | 从底部滑入 → 停留 2s → 向上淡出 |
| Tab 切换 | 瞬间切换 | 底部指示条滑动 200ms ease |
| 下拉刷新 | 不支持 | 拉下 → 弹性回弹 → 旋转器 |

### 5.4 Skeleton Loading（骨架屏）

当前加载状态只有按钮的 `disabled:opacity-60`。Apple 产品使用骨架屏表达"内容即将到达"。

```
骨架元素规格：
  background: var(--surface-tertiary)
  border-radius: 8px
  动画: shimmer（从左到右的渐变扫光）
    background-size: 200% 100%
    animation: shimmer 1.5s infinite
    gradient: linear-gradient(
      90deg,
      var(--surface-tertiary) 25%,
      var(--surface-secondary) 50%,
      var(--surface-tertiary) 75%
    )

骨架变体：
  text-line:  height 12px, width 随机 60-100%, margin-bottom 8px
  stat-block: height 48px, width 100%
  avatar:     height 40px, width 40px, border-radius full
  card:       height 120px, width 100%, border-radius 16px
```

**需要骨架屏的位置**：
- 首页精选卡加载中
- 钱包列表加载中（3 行骨架）
- 钱包详情页加载中（profile + 决策卡 + stat grid 骨架）
- Watchlist feed 加载中

---

## 6. 空状态设计

### 6.1 问题诊断

当前所有空状态都是一行灰色文字：
- `"No wallets followed yet."`
- `"No AI report yet. Trigger analysis via backend API."`
- `"No explicit risk warnings."`

Apple 的空状态设计原则：**空状态是引导用户的最佳时机，而非告知失败。**

### 6.2 重设计方案

每个空状态包含 3 个元素：**插图 + 标题 + 引导操作**。

```
空状态组件规格
──────────────

容器：
  display: flex, flex-col, items-center, justify-center
  padding: 48px 24px
  text-align: center

插图区：
  width: 120px, height: 120px
  opacity: 0.6
  margin-bottom: 16px
  （使用简单的 SVG 插图或 SF Symbol 大图标）

标题：
  text: title-3, label-primary
  margin-bottom: 4px

说明：
  text: subheadline, label-tertiary
  max-width: 280px
  margin-bottom: 20px

操作：
  一个 Tinted/Medium 按钮
```

**各场景空状态设计**：

| 场景 | 图标 | 标题 | 说明 | 操作 |
|------|------|------|------|------|
| Watchlist 空 | 星形图标 | 开始关注钱包 | 关注后你会收到这些钱包的重要变动通知 | [去探索钱包] |
| 无 AI 报告 | 脑/芯片图标 | AI 分析进行中 | 系统将自动为该钱包生成分析报告 | [手动触发分析] |
| 无匹配结果 | 搜索图标 | 没有找到匹配结果 | 尝试调整筛选条件或搜索关键词 | [清除筛选] |
| 无异常 | 对勾图标 | 一切正常 | 目前没有需要关注的异常信号 | 无 |
| 无近期动态 | 时钟图标 | 暂无新动态 | 当该钱包有新交易或分析更新时会显示在这里 | 无 |
| Feed 空 | 列表图标 | 事件流为空 | 你关注的钱包尚未产生新事件 | [去关注更多钱包] |

---

## 7. 图标系统

### 7.1 问题诊断

当前 **无图标库**。仅使用 emoji 字符（如 sidebar 的导航项可能用文字、按钮用文字标签）。Emoji 在不同平台/浏览器渲染不一致，且不支持动态着色。

### 7.2 推荐方案

采用 **Lucide Icons**（MIT 开源，与 Apple SF Symbols 风格一致，24x24 / 1.5px stroke）。

```bash
npm install lucide-react
```

**图标映射表**：

| 用途 | Lucide 图标名 | 替代的当前元素 |
|------|-------------|--------------|
| 发现/首页 | `Home` | 文字 "Dashboard" |
| 探索 | `Search` | 文字 "Wallets" |
| 我的跟踪 | `Star` | 文字 "Watchlist" |
| 更多 | `MoreHorizontal` | 无 |
| 关注 | `Star` (outline) → `Star` (fill) | 文字按钮 |
| 分享 | `Share` | 文字按钮 |
| AI 报告 | `Brain` | 文字 |
| 异常 | `AlertTriangle` | 文字 |
| 收益上升 | `TrendingUp` | 无 |
| 收益下降 | `TrendingDown` | 无 |
| 复制 | `Copy` → `Check`（成功后） | 文字 "Copy" |
| 展开/折叠 | `ChevronRight` (→rotate 90°) | `<details>` |
| 返回 | `ChevronLeft` | 无 |
| 筛选 | `Filter` | 无 |
| 排序 | `ArrowUpDown` | 文字 |
| 外部链接 | `ExternalLink` | 无 |
| 刷新 | `RefreshCw` | 无 |
| 钱包 | `Wallet` | 无 |
| 市场 | `BarChart3` | 无 |

**图标使用规范**：

```
尺寸标准：
  导航图标: 24px × 24px, strokeWidth 1.5
  行内图标: 16px × 16px, strokeWidth 2
  大图标（空状态）: 48px × 48px, strokeWidth 1

颜色规则：
  导航 active: var(--tint-blue)
  导航 inactive: var(--label-tertiary)
  行内: inherit（跟随文字颜色）
  语义: 使用对应 tint 色
```

---

## 8. 移动端体验优化

### 8.1 问题诊断

当前移动端是"响应式缩小"而非"移动端原生设计"：
- Sidebar 在移动端变为水平滚动导航条，7 个项挤在一行。
- 卡片内距不变，在小屏幕上显得拥挤。
- 表单输入框使用固定 `w-40`、`w-48`，不适配窄屏。
- 按钮触控目标不足 44px（`py-1` 按钮高度约 28px）。

### 8.2 关键优化

**触控目标**：
```
Apple 最低要求: 44pt × 44pt

需要修正的元素：
  Watch 按钮 (py-1 ≈ 28px) → 最小 height 44px
  Language 切换 (py-1 ≈ 26px) → 最小 height 36px（Segmented Control）
  Badge 标签（不可点击则不影响）
  列表行（p-3 ≈ 38px 总高度）→ min-height 44px
```

**底部 Tab Bar**：
```
取代当前的水平导航条
height: 49px + safe-area-inset-bottom
4 个 Tab: 发现 / 探索 / 跟踪 / 更多

每个 Tab:
  icon: 24px 居中
  label: 10px 在图标下方
  active: tint-blue
  inactive: label-tertiary
  tap area: 至少 full tab width × full height
```

**下拉刷新**（移动端优先）：
```
全局支持，利用 overscroll-behavior + custom pull indicator
动画: 弹性回弹感
indicator: 自定义旋转器, 20px
```

**Safe Areas**：
```css
/* globals.css */
body {
  padding-top: env(safe-area-inset-top);
  padding-bottom: env(safe-area-inset-bottom);
  padding-left: env(safe-area-inset-left);
  padding-right: env(safe-area-inset-right);
}
```

**输入框全宽**：
```
移动端所有输入框: width 100%（去掉 w-40, w-48）
桌面端保持固定宽度
```

---

## 9. 暗色模式

### 9.1 问题诊断

当前硬编码 `color-scheme: light`，不支持暗色模式。对于金融产品（用户可能长时间盯盘），暗色模式是基本需求。

### 9.2 实现策略

由于已在 2.1 节定义了完整的 CSS 变量双色方案，暗色模式实现只需：

1. 删除 `globals.css` 中的 `color-scheme: light` 硬编码。
2. 将所有 Tailwind 硬编码色替换为语义 Token（已在 2.1 节列出映射表）。
3. 添加 `<meta name="color-scheme" content="light dark">`。
4. 在 Header 增加主题切换器（三态：Light / Dark / System）。

**暗色模式特殊处理**：

```
阴影：暗色模式下阴影不可见，改用边框
  elevation-1 → 暗色下添加 border 1px var(--border)
  elevation-2+ → 暗色下添加 border 1px var(--separator) + 略亮背景

卡片背景：
  surface-secondary 暗色 = #1C1C1E（而非纯黑）
  这创造了微妙的层次感

正负值颜色：
  暗色下 tint-green 调亮为 #30D158
  暗色下 tint-red 调亮为 #FF453A
  确保在深色背景上的对比度 ≥ 4.5:1
```

---

## 10. 无障碍优化

### 10.1 当前差距

- 缺少 `focus-visible` 样式（键盘用户无法看到焦点位置）。
- 按钮缺少 `aria-label`（如 Watch 按钮只有文字，屏幕阅读器缺少上下文）。
- 颜色对比度未验证（`text-muted` #64748b 在白底上对比度约 4.6:1，刚过 AA）。
- 无跳过导航链接（Skip to content）。
- 图表/数据无替代文本。

### 10.2 关键修复

```
Focus Visible（全局）：
  *:focus-visible {
    outline: 3px solid var(--tint-blue);
    outline-offset: 2px;
    border-radius: inherit;
  }

  *:focus:not(:focus-visible) {
    outline: none;
  }

ARIA Labels：
  Watch 按钮:    aria-label="Follow wallet {name}"
  Share 按钮:    aria-label="Share wallet {name}"
  AI 触发按钮:    aria-label="Trigger AI analysis for {name}"
  Acknowledge:   aria-label="Acknowledge anomaly alert"
  Pool Badge:    aria-label="{tier} tier wallet"

Reduce Motion：
  @media (prefers-reduced-motion: reduce) {
    *, *::before, *::after {
      animation-duration: 0.01ms !important;
      transition-duration: 0.01ms !important;
    }
  }

Color Contrast：
  label-tertiary 暗色下: 确保 ≥ 4.5:1 (WCAG AA)
  tint 色在 surface 上: 确保 ≥ 3:1 (UI components)

Skip to Content：
  <a href="#main" class="sr-only focus:not-sr-only ...">
    Skip to content
  </a>
```

---

## 11. 实施路径

### Phase 0: 设计基础设施（3 天）

| 任务 | 涉及文件 | 说明 |
|------|---------|------|
| 建立 CSS 变量色彩系统 | `globals.css` | 明/暗双色 Token |
| 扩展 Tailwind 配置 | `tailwind.config.ts` | 排版、间距、圆角、阴影 Token |
| 安装 Lucide Icons | `package.json` | `npm i lucide-react` |
| 创建 Skeleton 组件 | `components/ui/Skeleton.tsx` | 骨架屏基础组件 |
| 创建 Button 组件 | `components/ui/Button.tsx` | 5 种变体 × 4 种尺寸 |
| 创建 Badge 组件 | `components/ui/Badge.tsx` | Tier/Status/Category 变体 |
| 创建 Card 组件 | `components/ui/Card.tsx` | Grouped/Prominent/Material 变体 |
| 创建 StatCell 组件 | `components/ui/StatCell.tsx` | 数字展示 + 正负色 |
| 创建 EmptyState 组件 | `components/ui/EmptyState.tsx` | 插图 + 标题 + 操作 |

### Phase 1: 核心页面改版（1 周）

| 任务 | 优先级 | 说明 |
|------|--------|------|
| Header + 导航重构 | P0 | 桌面导航 + 移动端 Tab Bar |
| 布局系统调整 | P0 | 去掉 Sidebar，改为 Header + Content + TabBar |
| 钱包详情页改版 | P0 | 决策卡首屏 + 折叠区 + 动效 |
| 钱包卡片组件 | P0 | 统一 WalletCard，替换各页面散落实现 |
| 暗色模式支持 | P0 | CSS 变量已就位，全局色替换 |

### Phase 2: 完整页面改版（1 周）

| 任务 | 优先级 | 说明 |
|------|--------|------|
| 首页重设计 | P1 | 今日精选 + 信号 + 组合包 |
| 探索页合并 | P1 | 列表+排行榜合并 + Tab 筛选 |
| Watchlist 升级 | P1 | 动作流 + 组合概览 |
| 空状态全局替换 | P1 | 6 个空状态场景 |
| 骨架屏全局部署 | P1 | 所有异步加载区域 |

### Phase 3: 体验打磨（持续）

| 任务 | 优先级 | 说明 |
|------|--------|------|
| 微交互动效 | P2 | 按钮、折叠、列表入场 |
| 分享落地页 | P2 | `/s/[id]` 轻量页面 |
| 无障碍审计 | P2 | focus-visible、ARIA、对比度 |
| 性能优化 | P2 | View Transition、lazy loading |
| 手势支持 | P3 | 下拉刷新、左滑操作 |

---

## 12. 设计审查清单

每次提交 UI 变更前，用此清单验证：

- [ ] 所有颜色使用语义 Token，不使用硬编码 hex/Tailwind 色
- [ ] 文字层级不超过 4 级（标题/正文/辅助/最小）
- [ ] 卡片间距 ≥ 24px，卡片内距 ≥ 16px
- [ ] 所有可交互元素高度 ≥ 44px
- [ ] 状态变化有过渡动画（≥ 150ms）
- [ ] 异步操作有骨架屏或 loading 指示器
- [ ] 空状态有引导性内容（非纯文字）
- [ ] 暗色模式下视觉表现正常
- [ ] `prefers-reduced-motion` 下动画被禁用
- [ ] 所有图标使用 Lucide，不使用 emoji
- [ ] 正负数值使用语义色着色
- [ ] Focus-visible 样式可见且合理
